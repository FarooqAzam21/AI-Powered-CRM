Analyze the following email and classify its intent into a single category.

### Email Subject:
{{ subject }}

### Email Body:
{{ body }}

Return ONLY a JSON object strictly following this schema:
{
    "category": "string (e.g., support, sales, marketing, generic)"
}
